# API 변경 사항 안내 (프론트엔드 개발자용)

안녕하세요. 백엔드 API의 일관성, 보안, 안정성을 높이기 위해 `template` 관련 API의 일부 기능이 개선되었습니다.
아래 변경 사항을 확인하시고 프론트엔드 코드에 반영 부탁드립니다.

---

## 1. "내 템플릿 생성" API 변경

새로운 템플릿을 생성하는 API의 로직이 크게 변경되었습니다. 이제 여러 이메일 계정에 한 번에 템플릿을 생성할 수 있습니다.

- **Endpoint:** `POST /api/template/mytemplate/create/`
- **주요 변경 내용:**
    1.  **파라미터 위치 변경**: 쿼리 파라미터(`?key=value`) 방식에서 **요청 본문(Request Body)**으로 데이터 전달 방식이 변경되었습니다.
    2.  **다중 생성 지원**: 단일 `email_account_id` 대신, `email_account_ids` 라는 키로 **ID의 배열(리스트)**을 전달합니다.
    3.  **사용자 식별 방식 변경**: `user_id`를 직접 전달받지 않고, **인증 토큰(`Authorization` 헤더)을 통해** 서버가 사용자를 자동으로 식별합니다.

### 요청 예시 (JavaScript - axios)

#### - AS-IS (이전 방식)

```javascript
// 이전 방식: Query Parameter 사용, 단일 생성만 가능
const userId = 1;
const emailAccountId = 10;

const templateData = {
  template_title: "템플릿 제목",
  template_content: "템플릿 내용입니다.",
  sub_category: "카테고리",
  topic: "주제"
};

// URL에 쿼리 파라미터를 직접 붙여서 요청
axios.post(
  `/api/template/mytemplate/create/?user_id=${userId}&email_account_id=${emailAccountId}`,
  templateData,
  {
    headers: { 'Authorization': 'Bearer YOUR_AUTH_TOKEN' }
  }
);
```

#### - TO-BE (새로운 방식)

```javascript
// 새로운 방식: Request Body 사용, 다중 생성 지원
const templateData = {
  // email_account_id -> email_account_ids (배열 형태)
  email_account_ids: [10, 11, 12], // 템플릿을 추가할 이메일 계정 ID 목록
  template_title: "템플릿 제목",
  template_content: "템플릿 내용입니다.",
  sub_category: "카테고리",
  topic: "주제"
};

// 모든 데이터를 Request Body에 담아서 요청
axios.post(
  '/api/template/mytemplate/create/',
  templateData,
  {
    headers: { 'Authorization': 'Bearer YOUR_AUTH_TOKEN' }
  }
);
```

---

## 2. "공용 템플릿 가져오기" API 변경

공용 템플릿을 '내 템플릿'으로 복사해오는 API의 보안이 강화되었습니다.

- **Endpoint:** `POST /api/template/viewtemplate/<template_pk>/`
- **주요 변경 내용:**
    - **사용자 ID 파라미터 제거**: 기존에 요청 본문에 포함했던 사용자 `id` 필드가 제거되었습니다. 서버가 인증 토큰으로 사용자를 자동 식별하므로 더 이상 필요하지 않습니다.

### 요청 예시 (JavaScript - axios)

#### - AS-IS (이전 방식)

```javascript
// 이전 방식: Request Body에 사용자 id 포함
const requestBody = {
  id: 1, // 템플릿을 가져갈 사용자의 pk
  email_account_ids: [10, 11]
};

axios.post(
  '/api/template/viewtemplate/5/', // 5번 공용 템플릿을 가져오기
  requestBody,
  {
    headers: { 'Authorization': 'Bearer YOUR_AUTH_TOKEN' }
  }
);
```

#### - TO-BE (새로운 방식)

```javascript
// 새로운 방식: Request Body에서 사용자 id 제거
const requestBody = {
  email_account_ids: [10, 11] // 어느 계정으로 가져올지만 전달
};

axios.post(
  '/api/template/viewtemplate/5/', // 5번 공용 템플릿을 가져오기
  requestBody,
  {
    headers: { 'Authorization': 'Bearer YOUR_AUTH_TOKEN' }
  }
);
```

---

## 중요 공지

- **인증 헤더 필수**: 위 변경된 API들을 포함하여, 이제 대부분의 `template` 관련 API는 HTTP 요청 시 `Authorization: Bearer <TOKEN>` 헤더를 필수로 요구합니다. 로그인 및 인증 처리를 다시 한번 확인해 주세요.

궁금한 점이 있으시면 언제든지 백엔드 팀에 문의 바랍니다.
